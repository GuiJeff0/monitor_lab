# Environment files with local secrets
.env.local
.env.*.local
.env
# Logs and temporary files
*.log
*.tmp
*.temp
*.pid

# SSL / TLS certificates & private keys (keep directory structure via .gitkeep)
traefik/certificates/*
!traefik/certificates/.gitkeep

# Operating System files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE and Editor files
.idea/
.vscode/
*.swp
*~
*.bak

# Docker data mounts & temporary runtime data (if mounted locally)
/data/
*.db

# Terraform
.terraform/
terraform/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
*.tfvars.json
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# SOPS & Age private keys (SECRETS)
*.agekey
*.age
keys.txt
*age*keys.txt
*.key
*.pem

# Kubernetes local configs
kubeconfig
kubeconfig.yaml
k3s.yaml
